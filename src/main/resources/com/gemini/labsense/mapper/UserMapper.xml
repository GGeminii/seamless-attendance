<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gemini.labsense.mapper.UserMapper">


    <select id="findUserPermsList" resultType="java.lang.String">
        select perm_code
        from tb_role_permission rp
                 left join tb_permission p
                           on rp.perm_id = p.id
        where rp.role_id = (select role_id from tb_user where id = #{id})
    </select>
</mapper>